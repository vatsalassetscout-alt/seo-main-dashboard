{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "source": "/api/(.*)",
      "dest": "server.ts"
    },
    {
      "source": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
